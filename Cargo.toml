[package]
name = "razer-tray"
version = "0.1.0"
edition = "2021"
description = "Minimal tray indicator for Razer mouse battery via kernel sysfs"

[dependencies]
ksni = "0.2"
notify-rust = "4"
glob = "0.3"
